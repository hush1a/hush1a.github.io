{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.astro|*.ts|*.mjs|*.cjs) \"${CLAUDE_PROJECT_DIR:-.}/node_modules/.bin/eslint\" --fix \"$f\";; esac; } 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Linting edited file..."
          }
        ]
      }
    ]
  }
}
